# Source repository

repo: anbuxcapital/CrosswordDesign
branch: main

## Last sync

date: 2026-09-02T00:00:00Z

### Updated in this project
- Associated the empty `CrosswordDesign` repo as this project's source.
- Handoff package prepared for it: spec README + both HTML prototypes.
- Nothing imported yet — the repo has no commits on `main` (tree returns 409/empty).

## Screen map

| Project screen | Repo files |
|---|---|
| Feed, Browse, Play, Solved, Wallet, Profile, funnel (`Crosscut Prototype.dc.html`) | — (repo empty; built from the attached `IOS Crosswords` folder: `apps/app/app/index.tsx`, `apps/app/src/components/Grid.tsx`, `QuestionBanner.tsx`, `apps/app/src/theme.ts`, `workers/gateway/src/puzzles/en/*.json`) |
| Exploration gallery (`Feed Explorations.dc.html`) | — |
